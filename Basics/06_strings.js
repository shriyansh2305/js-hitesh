const name = "shriyansh"
const repoCount = 5

// console.log(name + repoCount + " value") //not recommended

// console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

// console.log(typeof name);
// name is a string
// console.log(name.length);

const gameName = new String("contra-mario");
// console.log(typeof gameName);
// gameName is an string-object not array

// console.log(gameName.__proto__);

// console.log(gameName.toUpperCase());
// name[0]='k';
// gameName[0]='k';
// console.log(gameName);
// console.log(name);
// strings are immutable

// Some other methods: 
// charAt(index), indexOf(character), substring(start-idx, length), slice() -> can have -ve values, try once
// trim(), trimStart(), trimEnd() -> works for white spaces and line terminators

// replace(search_str, replace_str)
// includes("search-string") -> true/false
// split(' ' or '-') -> character on the basis of which we want to split -> returns an array





